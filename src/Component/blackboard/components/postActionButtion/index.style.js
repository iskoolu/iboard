import React from 'react';
import { 
    StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
    button: {
        flex:1,
        height: 40,
        flexDirection: "row",
        width: "100%",
        justifyContent: "center",
        alignItems: "center"
    },
    buttonText: {
        marginLeft: 10,
        alignSelf: "center",
        alignItems: "center"
    },
})
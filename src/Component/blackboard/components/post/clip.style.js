import React from 'react';
import {
    StyleSheet
} from 'react-native';

export const styles = StyleSheet.create({
    clip: { 
        paddingVertical: 1, 
        borderRadius: 50 
    },
    clipText: {
        fontSize: 12
    }
})